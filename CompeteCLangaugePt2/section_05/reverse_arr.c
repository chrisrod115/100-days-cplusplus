#include <stdio.h>
#include <stdlib.h>

#define SIZE 5

void swap(char* ptr1, char* ptr2){
    char temp;
    temp = *ptr1
    *ptr1 = *ptr2
    
}

char func(char* arr, int* size){

}

int main(){
    char arr[SIZE] = {'a','b','c','d','e'};
    func(arr, SIZE)
    return 0;
}
